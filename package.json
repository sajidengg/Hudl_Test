{
  "name": "hudl_test",
  "version": "1.0.0",
  "description": "Automated the Login functionality with different test cases",
  "main": "index.js",
  "scripts": {
    "test": "NOW=$(date \"+%m-%d-%Y-%T\") && mocha  Pages/**.js  --parallel --no-timeouts  --reporter mochawesome --require mochawesome/register  --reporter-options reportDir=Test_Report, --reporter-options reportFilename=test-${NOW}"
  },
  "author": "Sajidali",
  "license": "ISC",
  "dependencies": {
    "chai": "^4.3.6",
    "chromedriver": "^98.0.1",
    "geckodriver": "^3.0.1",
    "mocha": "^9.2.0",
    "mochawesome": "^7.0.1",
    "selenium-webdriver": "^4.1.1"
  }
}
